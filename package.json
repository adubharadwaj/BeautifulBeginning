{
  "name": "beautifulbeginning",
  "version": "1.0.0",
  "description": "Homepage",
  "main": "server.js",
  "homepage": "home.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Adithya",
  "license": "ISC",
  "devDependencies": {    
    "http-server": "^0.9.0"    
  },
  "scripts": {
    "postinstall": "bower install",

    "prestart": "npm install",
    "start": "http-server ./app -a localhost -p 8000 -c-1"
  }
}
